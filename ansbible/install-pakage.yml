--- # installing software
  - hosts: all
    user: ansible
    become: true
    connection: ssh

    tasks:
      - name: installing software into hosts
        yum:
          name: docker
          state: installed

